1) Базовый и индексный – адресация допустима только через регистры EBХ, EВР, ESI и EDI. При использовании регистров EBХ или EBР адресацию называют базовой, при использовании регистров ESI или EDI – индексной. При адресации через регистры EBХ, ESI или EDI в качестве сегментного регистра подразумевается DS; при адресации через EВР – регистр SS. Таким образом, косвенная адресация через регистр EВР предназначена для работы со стеком.
2) Базовый и индексный со смещением – относительный адрес операнда определяется суммой содержимого регистра (EBХ, EВР, ESI или EDI) и указанного в команде числа, которое называют смещением (сдвигом): mov ax, [ ebp ]+2
3)  Базово-индексный (адресация по базе с индексированием) – относительный адрес операнда определяется как сумма содержимого пар регистров EBХ, EВР, ESI или EDI : mov ax, [ebp+esi]
4) Базово-индексный со смещением – относительный адрес операнда определяется как сумма содержимого пар регистров EBХ, EВР, ESI или EDI и смещения:

mov ax,[ebx+esi+2]

mov ax,[ebx][esi]+2

mov ax,[ebx+2][esi]

mov ax,[ebx][esi+2]

mov ax,2[ebx][esi]

Это все записи одного и того же действия:

В регистр AX помещается слово из ячейки памяти со смещением, равным сумме чисел, содержащихся в EBX и ESI, и числа 2

5) Косвенная адресация с масштабированием: Идентичен предыдущему, за исключением того,|

что с его помощью можно прочитать элемент массива слов, двойных слов или учетверенных слов:

mov ax,[esi*2]+2
## Работа с одномерными массивами
 Для работы с одномерным массивом мы используем косвенную адрессацию
 пример работы с массивом есть в [[Решения задач]]
 