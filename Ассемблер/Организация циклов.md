Определений и пояснений нет(только примеры)
## Примеры
```
Цикл
whil(boolop){...}
asm:
metka:
   cmp eax,ebx
   jxx metkaVix
   ...тело функции
   jmp metka
metkaVix
do while
metka:
     тело
     cmp оп1,оп2
     jxx metkaVix
     jmp metka
metkaVix:
```
#ЦиклСПомощьюLOOP
## Loop
Loop адрес
Считается всё от ecx уменьшая его на единицу он переходит на адрес(метку) которая указана (это при условии что ecx не равен нулю после операции)
```
mov ecx, N
cicle1:
    //telo
    loop cicle1
endcicle:
```
Команда LOOP относится к ближнему прямому
типу перехода (для 16-битного режима – к короткому
прямому типу). 
LOOPE и LOOPNE 
(проверяет флаги на (ZF) аналогично с прыжкам  :)
XD XD XD XD XD XD XD XD